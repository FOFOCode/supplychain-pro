# Filtro Fail2ban — detecta escaneos masivos de rutas inexistentes (404 spam)
# Bloquea IPs que acumulan muchos errores 404 seguidos

[Definition]
failregex = ^<HOST> .+ "(GET|POST|PUT|DELETE|HEAD|OPTIONS) \S+ HTTP/\S+" 404 .+$
ignoreregex =
